name: CI
on: push
jobs:
    lint:
        runs-on: self-hosted
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Install deps
              run: yarn install --frozen-lockfile
            - name: Lint
              run: yarn lint:check
